# Get Started

1. Create a Python venv and install deps (see `pyproject.toml`).
2. Configure models and routing in `config/model_mapping.yaml` and `config/agent_config.yaml`.
3. Run `python -m core.orchestrator` to start the orchestrator.
4. Populate RAG indices under `rag/domain_indices/*`.
